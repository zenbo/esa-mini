{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(echo $TOOL_INPUT | jq -r '.file_path // .filePath // empty') && if [ -n \"$file_path\" ] && echo \"$file_path\" | grep -q '\\.go$'; then gofmt -w \"$file_path\" 2>&1; fi"
          }
        ]
      }
    ]
  }
}
